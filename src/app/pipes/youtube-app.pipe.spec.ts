import { YoutubeAppPipe } from './youtube-app.pipe';

describe('YoutubeAppPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeAppPipe();
    expect(pipe).toBeTruthy();
  });
});
